\## hwcheck

A few concerns were made from the previous version that was a messy batch about security. hwcheck is now being ported into C# on top of the .NET library for more control and better security. Also some improvements will be added such as having a json output for automation , and a better UI using WPF .

